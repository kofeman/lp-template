//- Input control with form-group wrapper
mixin input(control,type,id,placeholder,label,name)
	.form-group
		label(for="#{id}") #{label}
		input.form-control(type="#{type}",id="#{id}",placeholder="#{placeholder}",name="#{name}",class='#{control}')
